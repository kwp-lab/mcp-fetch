startCommand:
  type: stdio
  configSchema:
    # JSON Schema defining the configuration options for the MCP server.
    {}
  commandFunction:
    # A function that produces the CLI command to run to start the MCP server on stdio.
    |-
    (config) => ({
      "command": "npx",
      "args": [
        "."
      ]
    })
